import 'package:flutter/material.dart';

class CreateLessonScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ders Oluştur Sayfası", style: TextStyle(fontSize: 24)),
    );
  }
}
